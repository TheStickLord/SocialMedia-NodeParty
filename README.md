Setup:

  **Important: In main directory, you need to manually create the folder "content". Within "content" create the folders "allPosts" and "newPosts".

Use:

  To run the webapp, run "node .\nartyDeploy.js"
  This will open the webapp on port 3000!

Access:

  Access at:
  <your IP>:3000
